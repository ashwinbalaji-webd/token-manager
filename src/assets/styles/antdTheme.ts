import { theme } from "antd";

export const themeToken = {
  token: {
    // Seed Token
    colorPrimary: "#D22635",
    borderRadius: 10,
    fontFamily: "Poppins"
    // // Alias Token
    // colorBgContainer: "black",
  },
  algorithm: theme.defaultAlgorithm,
};
